@using MudBlazor
@using MudBlazorDialogTest.Models.DtoModels


<MudDialog>
    <TitleContent>
        Add a Special Assignment Employee
    </TitleContent>

    <DialogContent>
        <MudForm Model="EmployeeToReturn">

            <MudFocusTrap>
                <MudTextField Variant="Variant.Outlined" Clearable="true" @bind-Value="EmployeeToReturn.SpecialAssignment.EmployeeId" Label="Employee ID" />
                <MudTextField Variant="Variant.Outlined" Clearable="true" @bind-Value="EmployeeToReturn.SpecialAssignment.SpecialAssignmentGroup" Label="Assignment Group" />
            </MudFocusTrap>

        </MudForm>
    </DialogContent>


    <DialogActions>
        <MudButton OnClick="OnCancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="OnSubmit">Ok</MudButton>
    </DialogActions>


</MudDialog>






@code {

    [CascadingParameter]
    private MudDialogInstance MudDialog { get; set; } = new MudDialogInstance();

   

    private DtoSpecialAssignmentEmployee EmployeeToReturn = new DtoSpecialAssignmentEmployee();

    private void OnSubmit() => MudDialog.Close(DialogResult.Ok(EmployeeToReturn));

    private void OnCancel() => MudDialog.Cancel();
}